# env.json

## tenant
tenant = "_main_" - Main web application
tenant = "_runtime_" - Determined by the subdoomain
tenant = "client1" - Uses the specific client configuration

## apiUrl
The base URL for the API endpoints.

## pyBackendUrl
The URL for the Python backend service.
